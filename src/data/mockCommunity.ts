import { UserProfile, GameLog, Review, CustomList } from '../types';

// Zero placeholder data for production readiness
export const DEFAULT_USER: UserProfile | null = null;

export const INITIAL_USER_LOGS: GameLog[] = [];

export const INITIAL_REVIEWS: Review[] = [];

export const INITIAL_LISTS: CustomList[] = [];
